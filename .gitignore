*.iml
.DS_Store
.cxx/
.externalNativeBuild/
.gradle/
/.idea/
/build/
/captures/
/local.properties
app/build/

# Release signing. The key and its passwords are held outside the repository;
# the build reads them from here or from the environment.
keystore.properties
*.jks
*.keystore

# Kotlin incremental build state.
.kotlin/
